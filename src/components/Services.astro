---
const services = [
  {
    name: 'Creativity',
    desc: 'Ideas valientes y memorables que venden y generan voz de marca.',
    featured: false,
  },
  {
    name: 'Business Strategy',
    desc: 'Estrategias de alto impacto que crean valor de negocio.',
    featured: false,
  },
  {
    name: 'Brave Lab',
    desc: 'Neurociencia aplicada: eye tracking, facial coding, tests de asociación implícita, UX y modelos predictivos. Única agencia en Chile con laboratorio propio.',
    featured: true,
    href: 'https://brave-lab.com',
  },
  {
    name: 'Brand Strategy · Design',
    desc: 'Diseño y estrategia de marca diferenciadora.',
    featured: false,
  },
  {
    name: 'Media · Analytics',
    desc: 'Estrategias de medios y análisis de métricas.',
    featured: false,
  },
  {
    name: 'Experience',
    desc: 'Experiencia de consumidor completa, de punta a punta.',
    featured: false,
  },
  {
    name: 'Digital · Now',
    desc: 'Soluciones digitales: web, juegos, metaverso e IA.',
    featured: false,
  },
  {
    name: 'Inside The Company',
    desc: 'Employer branding y comunicación interna para RRHH.',
    featured: false,
  },
];

const coffeeHref = `https://wa.me/56982190769?text=${encodeURIComponent('Hola, quiero agendar un café con The Boys')}`;
---

<section id="servicios" class="section pilares">
  <p class="section-kicker">Los servicios de The Boys, agencia de publicidad creativa digital en Chile y Latinoamérica</p>
  <h2 class="section-title">Nuestros servicios</h2>
  <div class="pilares-grid">
    {
      services.map((s) => (
        <article class:list={['pilar-card', { featured: s.featured }]}>
          <h3>{s.name}</h3>
          <p class="pilar-desc">{s.desc}</p>
          {s.href && (
            <a class="pilar-link" href={s.href} target="_blank" rel="noopener noreferrer">
              Ver Brave Lab
            </a>
          )}
        </article>
      ))
    }
    <article class="pilar-card cta-card">
      <h3>¿Tomamos un café?</h3>
      <p class="pilar-desc">Conversemos sobre tu próximo proyecto.</p>
      <a class="cta-card-link" href={coffeeHref} target="_blank" rel="noopener noreferrer">
        Agendar un café
      </a>
    </article>
  </div>
</section>

<style>
  .pilares-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1px;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.12);
  }
  .pilar-card {
    background: var(--ink);
    padding: var(--space-4);
    transition: background 0.2s ease;
  }
  .pilar-card:hover,
  .pilar-card:focus-within {
    background: #161616;
  }
  .pilar-card.featured {
    background: #14101c;
  }
  .pilar-card.featured:hover {
    background: #1a1424;
  }
  .pilar-link {
    display: inline-block;
    margin-top: var(--space-2);
    font-family: var(--font-body);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    font-size: 13px;
    color: var(--lila);
    text-decoration: underline;
  }
  .pilar-card h3 {
    font-family: var(--font-display);
    text-transform: uppercase;
    font-size: 20px;
    margin-bottom: var(--space-1);
    color: var(--paper);
  }
  .pilar-card.featured h3 {
    color: var(--lila);
  }
  .pilar-desc {
    font-size: 14px;
    line-height: 1.5;
    color: rgba(250, 250, 250, 0.65);
  }
  .cta-card {
    background: var(--lime);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
  }
  .cta-card:hover {
    background: var(--lime);
    filter: brightness(1.05);
  }
  .cta-card h3 {
    color: var(--ink);
  }
  .cta-card .pilar-desc {
    color: rgba(0, 0, 0, 0.7);
    margin-bottom: var(--space-2);
  }
  .cta-card-link {
    font-family: var(--font-body);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    font-size: 13px;
    color: var(--ink);
    text-decoration: underline;
  }
</style>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (!reduceMotion) {
    gsap.registerPlugin(ScrollTrigger);

    gsap.from('.pilar-card', {
      scrollTrigger: {
        trigger: '.pilares-grid',
        start: 'top 80%',
        once: true,
      },
      opacity: 0,
      y: 24,
      duration: 0.7,
      ease: 'power2.out',
      stagger: 0.06,
    });
  }
</script>
